import * as urls from './urls.config';
import * as thirdParty from './thirdParty.config';
import { company } from './company.config';

const config = { urls, thirdParty, company };

export default config;
export { urls, thirdParty, company };
